package net.noxumbrarum.lunaeclara.blocks.builder;

public enum BlockType
{
	/**
	 * Used for solid blocks (e.g. ingot Blocks, decorative Block).
	 * Full block with only one texture.
	 */
	SOLID,
	/**
	 * Blocks that don't occupy a full 1x1x1.
	 */
	Partial
}
